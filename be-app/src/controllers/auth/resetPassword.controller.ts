import type { Response, Request, NextFunction } from "express";

export const resetPasswordController = (
	req: Request,
	res: Response,
	next: NextFunction,
) => {
	try {
		
	} catch (error) {
		next(error)
	}
};
